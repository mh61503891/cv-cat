<!DOCTYPE html>
<html>
  <head>
    <title>CV</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>
  <body>
  <nav class="navbar navbar-expand-sm navbar-light bg-light">
    <div class="container-fluid">
      <%= link_to 'CV', root_path, class: %w[navbar-brand] %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to 'Papers', papers_path, class: %w[nav-link] %>
          </li>
          <li class="nav-item">
            <%= link_to 'Authors', authors_path, class: %w[nav-link] %>
          </li>
          <li class="nav-item">
            <%= link_to 'Publications', publications_path, class: %w[nav-link] %>
          </li>
          <li class="nav-item">
            <%= link_to 'Publishers', publishers_path, class: %w[nav-link] %>
          </li>
          <li class="nav-item">
            <%= link_to 'Keywords', keywords_path, class: %w[nav-link] %>
          </li>
        </ul>
      </div>
    </div>
  </nav>
    <div class="container mt-3">
      <%= yield %>
    </div>
  </body>
</html>
